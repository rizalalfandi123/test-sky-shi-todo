import { styled } from "@mui/material/styles";
import Box, { BoxProps } from "@mui/material/Box";

export const NotificationConatiner = styled(Box)<BoxProps>({
  padding: "20px 30px",
  display: "flex",
  alignItems: "center",
  gap: "13px",
});
